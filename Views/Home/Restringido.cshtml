@{
    ViewData["Title"] = "Acceso Denegado";
}

<div class="d-flex flex-column justify-content-center align-items-center vh-100 text-center">
    <div class="mb-4">
        <i class="bi bi-shield-lock-fill" style="font-size:6rem;color:#dc3545;"></i>
    </div>

    <h1 class="display-4 text-danger mb-3">🚫 Acceso Denegado</h1>
    <p class="lead mb-4">
        Lo sentimos, no tienes permiso para acceder a esta sección.
        <br/>
        Si crees que esto es un error, contacta con el administrador del sistema.
    </p>

    @* Botón para volver al dashboard según rol *@
    @if (User.Identity != null && User.Identity.IsAuthenticated)
    {
        @if (User.IsInRole("Administrador"))
        {
            <a asp-controller="Home" asp-action="Index" class="btn btn-primary btn-lg">
                <i class="bi bi-house-door-fill"></i> Volver al Panel Principal
            </a>
        }
        else if (User.IsInRole("Empleado"))
        {
            <a asp-controller="OrdenesTrabajo" asp-action="MisOTs" class="btn btn-primary btn-lg">
                <i class="bi bi-house-door-fill"></i> Volver a Mis Órdenes
            </a>
        }
    }
    else
    {
        <a asp-controller="Usuarios" asp-action="Login" class="btn btn-primary btn-lg">
            <i class="bi bi-box-arrow-in-right"></i> Iniciar Sesión
        </a>
    }
</div>
