@model TallerBecerraAguilera.Models.Empleados

@{
    ViewData["Title"] = "Mi Dashboard";
}

<!-- Header -->
<div class="mb-8 flex flex-col md:flex-row md:items-center justify-between gap-4">
    <div>
        <h2 class="text-2xl font-bold text-gray-800">
            👷‍♂️ Bienvenido, @Model.NombreCompleto
        </h2>
        <p class="text-gray-500 text-sm">
            Resumen de tus órdenes de trabajo
        </p>
    </div>
</div>

<!-- Cards resumen -->
<div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">

    <!-- Pendientes -->
    <div class="bg-white p-6 rounded-2xl shadow-sm border border-gray-100 hover:shadow-md transition-shadow group">
        <div class="flex justify-between items-start">
            <div>
                <p class="text-xs font-semibold text-gray-400 uppercase tracking-wider mb-1">
                    Pendientes
                </p>
                <h3 class="text-3xl font-bold text-gray-800 group-hover:text-primary transition-colors">
                    @ViewBag.MisOTsPendientes
                </h3>
            </div>
            <div class="p-3 bg-indigo-50 text-primary rounded-xl">
                <i class="bi bi-hourglass-split text-xl"></i>
            </div>
        </div>
        <div class="mt-4 text-xs text-gray-400">
            Órdenes asignadas sin iniciar
        </div>
    </div>

    <!-- En curso -->
    <div class="bg-white p-6 rounded-2xl shadow-sm border border-gray-100 hover:shadow-md transition-shadow group">
        <div class="flex justify-between items-start">
            <div>
                <p class="text-xs font-semibold text-gray-400 uppercase tracking-wider mb-1">
                    En Curso
                </p>
                <h3 class="text-3xl font-bold text-gray-800 group-hover:text-orange-500 transition-colors">
                    @ViewBag.MisOTsEnCurso
                </h3>
            </div>
            <div class="p-3 bg-orange-50 text-orange-500 rounded-xl">
                <i class="bi bi-tools text-xl"></i>
            </div>
        </div>
        <div class="mt-4 text-xs text-gray-400">
            Vehículos en reparación
        </div>
    </div>

    <!-- Finalizadas -->
    <div class="bg-white p-6 rounded-2xl shadow-sm border border-gray-100 hover:shadow-md transition-shadow group">
        <div class="flex justify-between items-start">
            <div>
                <p class="text-xs font-semibold text-gray-400 uppercase tracking-wider mb-1">
                    Finalizadas
                </p>
                <h3 class="text-3xl font-bold text-gray-800 group-hover:text-emerald-500 transition-colors">
                    @ViewBag.MisOTsFinalizadas
                </h3>
            </div>
            <div class="p-3 bg-emerald-50 text-emerald-500 rounded-xl">
                <i class="bi bi-check-circle-fill text-xl"></i>
            </div>
        </div>
        <div class="mt-4 text-xs text-gray-400">
            Trabajos completados
        </div>
    </div>

</div>

<!-- Órdenes recientes -->
<div class="bg-white rounded-2xl shadow-sm border border-gray-100 overflow-hidden">
    <div class="px-6 py-4 border-b border-gray-100 flex justify-between items-center">
        <h3 class="font-bold text-gray-800">
            📋 Mis órdenes recientes
        </h3>
    </div>
    <div class="p-0">
        @await Component.InvokeAsync("UltimasOTsEmpleado", new { empleadoId = Model.Id })
    </div>
</div>
